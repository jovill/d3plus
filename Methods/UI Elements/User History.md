#### <a name="boolean" href="#boolean">.history( *boolean* )</a>

Displays a back button in the top left of the visualization when the user has zoomed to a deeper nesting level. Defaults to ```true```.
